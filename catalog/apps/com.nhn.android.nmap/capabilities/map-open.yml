catalog_version: "0.1"
app_id: "android:com.nhn.android.nmap"

capability:
  local_slug: "map-open"

  semantic:
    normalized_key: "app.open.home"
    status: "confirmed"
    hypotheses: []

  title: "Open NAVER Map"
  summary: "Open NAVER Map, optionally centered to a coordinate and zoom."

  preconditions:
    app_installed: true
    login_required: false
    location_permission_required: false
    region_lock: "KR-centric service"

  expected_outcome:
    landing: "map_view"
    fallback_behavior: "Without coordinates, opens default map surface"

  risk:
    side_effect_class: "navigation_only"
    user_confirmation_expected: false
    agent_safe_to_attempt: true

  status:
    evidence: "official_doc"
    runtime: "untested"
    freshness: "fresh"

  confidence: "medium"

  surfaces:
    - id: "nmap-map-v1"
      type: "custom_scheme"
      uri_template: "nmap://map?lat={lat}&lng={lng}&zoom={zoom}&appname={appname}"
      intent:
        action: "android.intent.action.VIEW"
        package_name: "com.nhn.android.nmap"
      routing:
        may_show_chooser: false
        notes: "Official doc also shows bare nmap://map?&appname=..."

  verification:
    last_verified_at: null
    samples: []

  evidence:
    - id: "official-doc-url-scheme"
      type: "official_doc"
      path: "../evidence/2026-03-19-official-url-scheme.md"
      url: "https://guide.ncloud-docs.com/docs/en/maps-url-scheme"
      observed_at: "2026-03-19T16:20:00+09:00"

  notes:
    - "Promote to verified after black-box launch succeeds on a user-owned device."
