catalog_version: "0.1"
app_id: "android:com.nhn.android.nmap"

capability:
  local_slug: "route-drive"

  semantic:
    normalized_key: "route.drive"
    status: "confirmed"
    hypotheses: []

  title: "Open driving route"
  summary: "Open NAVER Map driving directions between start and destination points."

  preconditions:
    app_installed: true
    login_required: false
    location_permission_required: false
    region_lock: "KR-centric service"

  expected_outcome:
    landing: "route_driving"
    fallback_behavior: "Route quality and alternate route handling depend on app version and map coverage"

  risk:
    side_effect_class: "navigation_only"
    user_confirmation_expected: false
    agent_safe_to_attempt: true

  status:
    evidence: "official_doc"
    runtime: "untested"
    freshness: "fresh"

  confidence: "medium"

  surfaces:
    - id: "nmap-route-car-v1"
      type: "custom_scheme"
      uri_template: "nmap://route/car?slat={slat}&slng={slng}&sname={sname}&dlat={dlat}&dlng={dlng}&dname={dname}&appname={appname}"
      intent:
        action: "android.intent.action.VIEW"
        package_name: "com.nhn.android.nmap"
      routing:
        may_show_chooser: false
        notes: "Official doc also shows optional ecoords and waypoint parameters"

  verification:
    last_verified_at: null
    samples: []

  evidence:
    - id: "official-doc-url-scheme"
      type: "official_doc"
      path: "../evidence/2026-03-19-official-url-scheme.md"
      url: "https://guide.ncloud-docs.com/docs/en/maps-url-scheme"
      observed_at: "2026-03-19T16:20:00+09:00"

  notes:
    - "Waypoint support should probably become a separate richer capability later."
